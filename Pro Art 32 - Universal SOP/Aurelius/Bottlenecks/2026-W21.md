---
date: 2026-05-21
week: 2026-W21
type: bottleneck-scan
agent: bottleneck_agent
generated_at: 2026-05-21T13:57:24.530777
tags: [bottleneck, performance, aurelius]
related: "["[[index]]", "[[Bottlenecks-and-Solutions]]"]"
---
# Bottleneck Scan — 2026-W21

Linked: [[index]] · [[Bottlenecks-and-Solutions]]

## Metrics summary

```
LLM: 11 samples, avg 9.8s, max 13.8s, ~3.1 tok/s
Whisper: 13 samples, avg 1.37s, max 3.70s
TTS first chunk: 34 samples, avg 1640ms
Barge-ins: 2
Wake hits: 4
Safety-gate trips: 0
Error-pattern hits: 27
```

## Findings

**Finding**: The system is experiencing frequent and recurring connectivity failures when calling the LLM service.
**Evidence**: Multiple instances of `[voice] Agent error: InternalServerError: litellm.InternalServerError: InternalServerError: OpenAIException - Connection error.` are present, indicating unstable network communication with the external API.
**Suggested fix**: Implement robust retry logic (e.g., exponential backoff) around all LLM calls to handle transient connection errors gracefully. Consider adding a circuit breaker pattern if failures persist.

**Finding**: Multiple critical components and scripts are failing initialization due to model unavailability or dependency issues.
**Evidence**: The logs show `[upgrade] not ready yet: Error code: 400 - {'error': 'Model is unloaded.'}` appearing multiple times, alongside failed checks like `LLM warmup (aurelius-full) skipped: Error code: 400 - {'error': 'Model is unloaded.'}`.
**Suggested fix**: Verify that the model (`aurelius-full`) is correctly loaded and available in memory *before* any service attempts to use it, potentially adding a dedicated health check endpoint for model readiness.

**Finding**: There are multiple Python dependency/import errors preventing full system initialization.
**Evidence**: Failures include `ImportError: module va not in sys.modules` and `ModuleNotFoundError: spec not found for the module 'va'`, as well as a critical syntax error in the daily review script (`SyntaxError: (unicode error) 'unicodeescape' codec can't decode bytes...`).
**Suggested fix**: Update the environment setup to ensure all required modules (like `va`) are installed and accessible. For the daily review script, escape the problematic unicode characters or use raw strings (`r"..."`) when defining paths/strings containing backslashes.

**Priority**: P0 (System instability due to connectivity loss and inability to initialize core components.)
