# 🚀 AURELIUS BOOTSTRAP — Read From Agent-Stack

This file exists at the vault root as a pointer.

**The real BOOTSTRAP run-of-show lives at:**

```
aurelius-agent-stack/BOOTSTRAP.md
```

i.e. **<https://github.com/xprimordius/aurelius-agent-stack/blob/main/BOOTSTRAP.md>**

---

## If you landed in the vault repo first

The vault is the DATA. The agent-stack repo has the master bootstrap instructions.

Run:

```bash
cd ..   # back up to AURELIUS_ROOT
gh repo clone xprimordius/aurelius-agent-stack
cat aurelius-agent-stack/BOOTSTRAP.md
```

Then follow what BOOTSTRAP.md says.

---

## The one master prompt (also documented in agent-stack/BOOTSTRAP.md §"THE ONE MASTER PROMPT")

```
You're booting Aurelius on this device. Follow the BOOTSTRAP.md run-of-show.

If ~/aurelius/aurelius-agent-stack does NOT exist (or C:/aurelius/aurelius-agent-stack on Windows), first run:
  mkdir -p ~/aurelius && cd ~/aurelius
  gh repo clone xprimordius/aurelius-agent-stack
  gh repo clone xprimordius/aurelius-vault

Then read ~/aurelius/aurelius-agent-stack/BOOTSTRAP.md and execute every step exactly as written.
```

Paste that into any new Claude Code session on any device. Claude takes it from there.

#bootstrap #pointer
